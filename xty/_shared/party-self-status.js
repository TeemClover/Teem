/* Daily self-status belongs on /xty/ notebook rows, underneath each companion.
   This module intentionally does nothing on /xty/p/. Kept as a compatibility
   stub because older cached card-ui bundles may still import this path. */
