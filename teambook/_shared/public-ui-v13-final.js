/* Retired compatibility entrypoint.
   Old cached bootstraps may still import this V1.3 URL. Keep the URL alive but
   delegate to the canonical Public detail renderer, which never guesses N/5. */

import './public-detail-v15.js?v=20260824-capacity-canon1';
