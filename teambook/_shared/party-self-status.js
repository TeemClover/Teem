/* Daily self-status belongs on / notebook rows, underneath each companion.
   This module intentionally does nothing on /p/. Kept as a compatibility
   stub because older cached card-ui bundles may still import this path. */
