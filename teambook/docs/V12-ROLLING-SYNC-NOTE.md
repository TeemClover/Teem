# TeamBook V1.2 rolling sync note

This branch is deliberately allowed to land during prelaunch while unfinished product-completion work remains tracked in GitHub Issue #163.

Source-of-truth rollout gates live in #163, not in chat memory. Safety snapshot branches must be preserved until production verification is complete.

The branch is periodically synced from current `main` because TeamBook cosmetic/onboarding work continues in parallel. A sync is not permission to drop V1.2 behavior; after each sync, compare #163 and preserve both current-main behavior and the V1.2 Ending/Collection/reusable-card work.
