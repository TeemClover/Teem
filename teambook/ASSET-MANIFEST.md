# TeamBook asset manifest

The canonical V5 manifest is [`docs/ASSET-MANIFEST.md`](docs/ASSET-MANIFEST.md).
Keep this pointer for migration-history links; runtime and deployment checks use
the canonical document and scripts under `scripts/`.

The optional `assets/decor/sticker`, `assets/decor/stationery`, and
`assets/decor/doodle` batches are not referenced by the bootable app.
