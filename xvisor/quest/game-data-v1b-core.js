export * from './game-data-v1a.js?v=1.0a-core';
import * as base from './game-data-v1a.js?v=1.0a-core';
export const GAME_VERSION='X-VISOR QUEST 1.0b';
export const RELEASE_VERSION='1.0b';
export const V1_SAVE_VERSION='1.0b';
export const V1_SCORE_VERSION='1.0b';
export const XGEN_SINGLE_MONTH_TARGET=3_000_000;
export const ORGANIZATION_START_MONTH=13,ORGANIZATION_END_MONTH=24;
export const ORGANIZATION_XIRCLE_MONTHS=Object.freeze([15,18,21,24]);
export const TRAVEL_MONTHS=Object.freeze([16,22]);
export const TRAVEL_DESTINATIONS=Object.freeze(['Tokyo','Seoul','Shanghai','Taipei','Paris','Dubai','Santorini','London','Cruise']);
export const EVENTS=base.EVENTS;
const FULL_START_XV=9_495,FULL_START_BAHT=12_480,ROUTINEX_XV=7_000,ROUTINEX_BAHT=7_490;
const n=v=>Math.max(0,Number(v||0));
function tier(sales){sales=n(sales);return sales>=100_000?{id:'25',label:'25%',rate:.25}:sales>=40_000?{id:'23',label:'23%',rate:.23}:{id:'20',label:'20%',rate:.20}}
export const getRetailTierBySalesBaht=tier;
export function getCurrentTGV(s){return Math.round(n(s?.economy?.personalXV)+n(s?.economy?.teamXV))}
function g1Rows(s){return (s.team||[]).filter(m=>m.active!==false&&m.parentId==='player').map(m=>{const personalXV=n(m.personalXV||m.monthlyOutput?.personalXV),salesBaht=n(m.personalSalesBaht||m.monthlyOutput?.personalSalesBaht),t=tier(salesBaht),commission=Math.round(personalXV*t.rate);return{id:m.id,name:m.name,personalXV,salesBaht,tier:t,commission,mentoring:Math.round(commission*.2)}})}
function qualified(s){return!!(s?.career?.xgenQualificationRule==='single-month'||s?.career?.xgenQualifiedSingleMonth||s?.campaignOutcome?.xgenByMonth12||s?.career?.xgenCertified1b)}
function xgenEffective(s){return qualified(s)||getCurrentTGV(s)>=XGEN_SINGLE_MONTH_TARGET}
export function calculateEconomy(s){const raw=base.calculateEconomy(s),personalXV=n(s?.economy?.personalXV),personalSalesBaht=n(s?.economy?.productSales||s?.economy?.personalSalesBaht),teamXV=n(s?.economy?.teamXV),tgv=Math.round(personalXV+teamXV),t=tier(personalSalesBaht),channel1=Math.round(personalXV*t.rate),rows=g1Rows(s),mentoringUnlocked=!!(s?.career?.xleadCertified||qualified(s)||['xlead','xgen'].includes(s?.rank)),channel2=mentoringUnlocked?rows.reduce((a,r)=>a+r.mentoring,0):0,channel3=xgenEffective(s)?Math.round(tgv*.05):0,projectedIncome=channel1+channel2+channel3,totalIncome=n(s?.economy?.totalIncome??s?.economy?.receivedIncome),closed=!!s?.settlements?.[String(s?.month)];return{...raw,personalXV,productSales:personalSalesBaht,personalSalesBaht,teamXV,tgv,currentTGV:tgv,tier,retailTier:t,retailRate:t.rate,channel1,channel2,channel3,channel4:0,directG1:rows,mentoringBreakdown:rows.map(r=>({name:r.name,commission:r.commission,mentorIncome:r.mentoring})),organizationIncome:channel3,projectedIncome,monthlyIncome:projectedIncome,teamIncome:channel2+channel3,totalIncome,receivedIncome:totalIncome,lifetimeIncome:totalIncome+(closed?0:projectedIncome)}}
function normalizeTeam(s){if(!s||!Array.isArray(s.team))return s;return{...s,team:s.team.map(m=>{const xv=n(m.personalXV||m.monthlyOutput?.personalXV),baht=n(m.personalSalesBaht||m.monthlyOutput?.personalSalesBaht),t=tier(baht),commission=Math.round(xv*t.rate);return{...m,retailTier:t.id,retailRate:t.rate,commission,monthlyOutput:m.monthlyOutput?{...m.monthlyOutput,commission}:m.monthlyOutput}})}}
function normalizeXgen(s,prev=null){if(!s)return s;const inherited=qualified(prev||s),hit=getCurrentTGV(s)>=XGEN_SINGLE_MONTH_TARGET&&!s.organizationMode&&Number(s.month||0)<=12,q=inherited||hit,first=Number(prev?.career?.xgenQualifiedAtMonth||s?.career?.xgenQualifiedAtMonth||(hit?s.month:0))||null,career={...(s.career||{}),xgenQualified:q,xgenQualifiedSingleMonth:q,xgenQualificationRule:q?'single-month':null,xgenQualifiedAtMonth:q?first:null,xgenCertified1b:q,xgenCertified:q};let rank=s.rank;if(q)rank='xgen';else if(rank==='xgen')rank=career.xleadCertified?'xlead':'xvisor';const just=hit&&!qualified(prev);return{...s,gameVersion:GAME_VERSION,releaseVersion:RELEASE_VERSION,v1SaveVersion:V1_SAVE_VERSION,scoreVersion:V1_SCORE_VERSION,rank,career,sceneReport:just?{kind:'xgen-qualified-1b',month:Number(s.month||0),tgv:getCurrentTGV(s),target:XGEN_SINGLE_MONTH_TARGET}:s.sceneReport,lastMessage:just?`🏆 XGEN Qualified · TGV เดือนนี้ ${getCurrentTGV(s).toLocaleString('th-TH')} XV · ③ 5% เริ่มในเดือนนี้ทันที`:s.lastMessage}}
function settlement(s){const e=calculateEconomy(s);return{month:Number(s.month||0),personalXV:Math.round(e.personalXV),personalSalesBaht:Math.round(e.personalSalesBaht),teamXV:Math.round(e.teamXV),currentTGV:Math.round(e.tgv),tgv:Math.round(e.tgv),retailRate:e.retailRate,retailTier:e.tier.id,channel1:Math.round(e.channel1),channel2:Math.round(e.channel2),channel3:Math.round(e.channel3),channel4:0,totalIncome:Math.round(e.projectedIncome),total:Math.round(e.projectedIncome),scoreVersion:V1_SCORE_VERSION,settled:true}}
function scoreFrom(s,settlements){const rows=Object.values(settlements||{}).filter(r=>Number(r.month)>=1&&Number(r.month)<=12),tgvs=rows.map(r=>n(r.currentTGV||r.tgv)),income=rows.map(r=>n(r.totalIncome??r.total)),ids=new Set();for(const p of [...(s.prospects||[]),...(s.customers||[]),...(s.team||[])])if(p?.personId||p?.id)ids.add(p.personId||p.id);return{locked:true,completedMonth:12,bestTgv:Math.max(0,...tgvs),totalIncome:income.reduce((a,b)=>a+b,0),bestMonthlyIncome:Math.max(0,...income),organizationSize:ids.size,completedAt:Date.now(),scoreVersion:V1_SCORE_VERSION,runMode:s.runMode||'FIRST_RUN',runId:s.runId,xgenByMonth12:!!s.career?.xgenQualifiedSingleMonth}}
function patchCampaignClose(before,after){const month=Number(before.month||0);if(month<1||month>12)return after;const qualifiedState=normalizeXgen(before,before),set=settlement(qualifiedState),sets={...(after.settlements||{}),[String(month)]:set},previous=n(before.economy?.totalIncome??before.economy?.receivedIncome),totalIncome=previous+set.totalIncome,history=[...(after.economy?.incomeHistory||[]).filter(r=>Number(r.month)!==month),{month,channel1:set.channel1,channel2:set.channel2,channel3:set.channel3,channel4:0,total:set.totalIncome,tgv:set.currentTGV}].sort((a,b)=>a.month-b.month);let next=normalizeXgen({...after,settlements:sets,economy:{...(after.economy||{}),totalIncome,receivedIncome:totalIncome,incomeHistory:history},organization:{...(after.organization||{}),lastMonthTGV:set.currentTGV,bestTGV:Math.max(n(after.organization?.bestTGV),set.currentTGV)}},before);if(month===12){const xgenByMonth12=!!(qualifiedState.career?.xgenQualifiedSingleMonth||set.currentTGV>=XGEN_SINGLE_MONTH_TARGET);next={...next,campaignOutcome:{...(next.campaignOutcome||{}),xgenByMonth12},campaignScore:{...scoreFrom(next,sets),xgenByMonth12}}}return next}
function pickIndex(s,salt,size){if(size<=1)return 0;let x=(Number(s.rngSeed||1)^(salt*2654435761))>>>0;x^=x<<13;x^=x>>>17;x^=x<<5;return Math.abs(x>>>0)%size}
function fixedTravel(s){if(!s?.organizationMode||s?.year2Path!=='xgen')return{...s,activeTravel:null,organization:{...(s.organization||{}),trips:[]}};const month=Number(s.month||0),existing=Array.isArray(s.organization?.trips)?s.organization.trips.filter(t=>TRAVEL_MONTHS.includes(Number(t.month))):[];if(!TRAVEL_MONTHS.includes(month))return{...s,activeTravel:null,organization:{...(s.organization||{}),trips:existing}};let trip=existing.find(t=>Number(t.month)===month);if(!trip){const used=new Set(existing.map(t=>t.destination)),available=TRAVEL_DESTINATIONS.filter(d=>!used.has(d)),destination=available[pickIndex(s,month+existing.length*17,available.length)]||TRAVEL_DESTINATIONS[0];trip={id:`trip-${month}-${destination.toLowerCase()}`,number:month===16?1:2,month,destination,title:`Recognition Trip ${month===16?1:2} · ${destination}`,earnedBy:'xgen-by-month12'}}const trips=[...existing.filter(t=>Number(t.month)!==month),trip].sort((a,b)=>a.month-b.month);return{...s,activeTravel:trip,organization:{...(s.organization||{}),trips},sceneReport:{kind:'travel-1b',month,trip},lastMessage:`✈️ ยินดีด้วย! ผลงานของทีมพาคุณผ่านทริป ${trip.destination} · ฉากนี้จะอยู่จนกดผ่านเดือน`}}
function year2(s){if(!s?.organizationMode)return s;const hit=!!(s.campaignOutcome?.xgenByMonth12||s.campaignScore?.xgenByMonth12),path=hit?'xgen':'xlead';let next={...s,year2Path:path,campaignOutcome:{...(s.campaignOutcome||{}),xgenByMonth12:hit}};if(hit)next={...next,rank:'xgen',career:{...(next.career||{}),xgenQualified:true,xgenQualifiedSingleMonth:true,xgenCertified:true,xgenCertified1b:true,xgenQualificationRule:'single-month'},organization:{...(next.organization||{}),cultureScore:Math.max(62,n(next.organization?.cultureScore)||62)}};else next={...next,rank:next.career?.xleadCertified?'xlead':'xvisor',career:{...(next.career||{}),xgenQualified:false,xgenQualifiedSingleMonth:false,xgenCertified:false,xgenCertified1b:false,xgenQualificationRule:null},organization:{...(next.organization||{}),cultureScore:Math.min(58,n(next.organization?.cultureScore)||52),trips:[]},activeTravel:null};return fixedTravel(next)}
function recalcYear2(before,after){const m=Number(before.month||0);if(!before.organizationMode||m<13||m>24)return after;const report=after.lastOrganizationReport||{},personalXV=n(report.personalXV??after.settlements?.[String(m)]?.personalXV),sales=n(report.personalSalesBaht??after.settlements?.[String(m)]?.personalSalesBaht),teamXV=n(report.teamXV??after.settlements?.[String(m)]?.teamXV),tgv=Math.round(n(report.tgv||personalXV+teamXV)),t=tier(sales),channel1=Math.round(personalXV*t.rate),rows=g1Rows(after),channel2=(before.career?.xleadCertified||['xlead','xgen'].includes(before.rank))?rows.reduce((a,r)=>a+r.mentoring,0):0,channel3=before.year2Path==='xgen'?Math.round(tgv*.05):0,total=channel1+channel2+channel3,set={...(after.settlements?.[String(m)]||{}),month:m,personalXV,personalSalesBaht:sales,teamXV,currentTGV:tgv,tgv,retailRate:t.rate,retailTier:t.id,channel1,channel2,channel3,channel4:0,totalIncome:total,total,scoreVersion:V1_SCORE_VERSION,settled:true},received=n(before.economy?.totalIncome??before.economy?.receivedIncome)+total,history=[...(after.economy?.incomeHistory||[]).filter(r=>Number(r.month)!==m),{month:m,channel1,channel2,channel3,channel4:0,total,tgv}].sort((a,b)=>a.month-b.month),fixedReport={...report,month:m,tgv,income:total,totalIncome:received,personalXV,personalSalesBaht:sales,teamXV,incomeBreakdown:{channel1,channel2,channel3},year2Path:before.year2Path};let next={...after,settlements:{...(after.settlements||{}),[String(m)]:set},economy:{...(after.economy||{}),totalIncome:received,receivedIncome:received,incomeHistory:history},lastOrganizationReport:fixedReport};if(after.twoYearSummary&&m===24){const all=Object.values(next.settlements||{}).filter(r=>Number(r.month)>=1&&Number(r.month)<=24);next={...next,twoYearSummary:{...after.twoYearSummary,month24TGV:tgv,year2EndTGV:tgv,month24Income:total,total24Income:received,totalIncome:received,bestTGV:Math.max(0,...all.map(r=>n(r.currentTGV||r.tgv))),bestMonthIncome:Math.max(0,...all.map(r=>n(r.totalIncome||r.total))),year2Path:before.year2Path,trips:before.year2Path==='xgen'?(next.organization?.trips||[]).filter(t=>TRAVEL_MONTHS.includes(Number(t.month))):[]}}}return year2(next)}
function correctFast(before,after,event){if(event!==EVENTS.FAST_TRACK_FULL_START)return after;const old=new Set((before.customers||[]).map(c=>c.personId||c.id));let found=null;const customers=(after.customers||[]).map(c=>{if(old.has(c.personId||c.id)||!c.fullSetFastLane)return c;found=c;return{...c,journey:'day0',status:'⚡ Full Start · Day 0 · เริ่มดูแลผลลัพธ์จริง',activePlan:true,customerState:base.CUSTOMER_STATES.NEEDS_HELP,day:0,followups:0,adherence:0,satisfaction:50,result:null,successCase:false,referralReady:false,xvisorInterest:false,xvisorStage:null,candidateProgress:0,selfDirected:false,measuredAgain:false}});if(!found)return after;return{...after,customers,monthStats:{...(after.monthStats||{}),successCases:Math.max(0,n(after.monthStats?.successCases)-1)},economy:{...(after.economy||{}),lastTransaction:{id:`tx-fast-${after.month}-${found.personId||found.id}-${Date.now()}`,status:'SIMULATION',price:FULL_START_BAHT,xv:FULL_START_XV,items:[{id:'xircle-starter',name:'Xircle Band + Scale',price:4_990,xv:2_495,cycle:'first'},{id:'routinex',name:'RoutineX',price:ROUTINEX_BAHT,xv:ROUTINEX_XV,cycle:'monthly'}]}},lastMessage:`✅ ${found.name} เริ่มครบชุดแล้ว · Day 0 · ต้องดูแลให้เกิดผลลัพธ์จริงก่อนเร่งเส้นทาง X-VISOR`}}
function patchTx(before,after){const tx=after?.economy?.lastTransaction;if(!tx)return after;const b=calculateEconomy(before),a=calculateEconomy(after),saleXV=n(tx.xv),sale=Math.round(saleXV*a.retailRate),oldXV=n(b.personalXV),trueUp=Math.max(0,Math.round(oldXV*(a.retailRate-b.retailRate))),d3=Math.max(0,a.channel3-b.channel3),d2=Math.max(0,a.channel2-b.channel2),delta=a.projectedIncome-b.projectedIncome;return{...after,economy:{...(after.economy||{}),lastTransaction:{...tx,incomeBefore:b.projectedIncome,incomeAfter:a.projectedIncome,incomeDelta:delta,salesBahtBefore:b.personalSalesBaht,salesBahtAfter:a.personalSalesBaht,tierBefore:b.tier,tierAfter:a.tier,incomeBreakdown:{saleChannel1:sale,tierTrueUp:trueUp,channel2Delta:d2,channel3Delta:d3,total:delta}}}}}
function release(s){return s?{...s,gameVersion:GAME_VERSION,releaseVersion:RELEASE_VERSION,v1SaveVersion:V1_SAVE_VERSION,scoreVersion:V1_SCORE_VERSION,campaignScore:s.campaignScore?{...s.campaignScore,scoreVersion:V1_SCORE_VERSION}:s.campaignScore}:s}
export const canOfferFullSetFastLane=(s,p)=>base.canOfferFullSetFastLane(s,p),getFastTrackChance=(s,p)=>base.getFastTrackChance(s,p);
export function makeInitialState(o={}){return release(normalizeXgen({...base.makeInitialState(o),campaignOutcome:{xgenByMonth12:false},year2Path:null,activeTravel:null}))}
export function makeNewGamePlusState(o={}){const s=base.makeNewGamePlusState(o);return release({...s,runMode:'NEW_GAME_PLUS',month:1,phase:'management',stage:base.STAGES.MANAGEMENT,rank:'xvisor',energy:base.MAX_ENERGY,prospects:[],customers:[],team:[],missions:[],campaignOutcome:{xgenByMonth12:false},year2Path:null,activeTravel:null,career:{...(s.career||{}),certificationPreviouslyPassed:true,xleadQualified:false,xleadCertified:false,xgenQualified:false,xgenQualifiedSingleMonth:false,xgenCertified:false,xgenCertified1b:false,xgenQualificationRule:null},economy:{...(s.economy||{}),personalXV:0,teamXV:0,productSales:0,teamProductSales:0,totalIncome:0,receivedIncome:0,incomeHistory:[],lastTransaction:null},settlements:{},lastMessage:'⚡ NEW GAME+ · Month 1 · ไม่มีลูกค้าตั้งต้น แต่เปิด Management เต็มรูปแบบทันที'})}
export function getBestNextActions(s,limit=3){if(s?.runComplete)return[{type:'new-game-plus',event:EVENTS.NEW_GAME_PLUS,label:'⚡ NEW GAME+',cost:0,score:5000}];if(s?.organizationMode)return[{type:'organization-pass',event:EVENTS.END_MONTH,label:'▶ ผ่านไปอีก 1 เดือน',cost:0,score:1000}];const actions=base.getBestNextActions(s,Math.max(limit,3))||[];if(s?.runMode==='NEW_GAME_PLUS'&&Number(s.month||0)===1&&!(s.prospects?.length||s.customers?.length||s.team?.length)){const useful=actions.filter(a=>a?.event);if(useful.length)return useful.slice(0,limit);return[{type:'lead',event:EVENTS.CREATE_LEAD,source:'known',payload:{source:'known'},label:'💬 รู้จักคนใหม่',cost:1,score:300},{type:'content',event:EVENTS.CREATE_LEAD,source:'content',payload:{source:'content'},label:'📣 ทำ Content เติม Pipeline',cost:1,score:250},{type:'end-month',event:EVENTS.END_MONTH,label:'🌙 จบเดือน',cost:0,score:20}].slice(0,limit)}return actions.slice(0,Math.max(1,limit))}
export function canDispatch(s,e){if(e===EVENTS.XGEN_EXAM&&!qualified(s)&&getCurrentTGV(s)<XGEN_SINGLE_MONTH_TARGET)return false;return base.canDispatch(s,e)}
export function reduceGame(current,event,payload={}){let before=release(normalizeXgen(current,current));if(event===EVENTS.NEW_GAME_PLUS)return makeNewGamePlusState({seed:Number(before.rngSeed||1)+101,previousScore:before.campaignScore||before.twoYearSummary?.campaignScore||null});if(before.organizationMode)before=year2(before);let after=normalizeTeam(base.reduceGame(before,event,payload));after=correctFast(before,after,event);after=patchTx(before,after);if(!before.organizationMode&&event===EVENTS.END_MONTH&&Number(before.month||0)<=12)after=patchCampaignClose(before,after);else after=normalizeXgen(after,before);if(event===EVENTS.ENTER_ORGANIZATION||after.organizationMode)after=year2(after);if(before.organizationMode&&event===EVENTS.END_MONTH){after=recalcYear2(before,after);after=fixedTravel(after)}if(after.runComplete&&after.twoYearSummary){const path=before.year2Path||after.year2Path||(after.campaignOutcome?.xgenByMonth12?'xgen':'xlead');after={...after,year2Path:path,twoYearSummary:{...after.twoYearSummary,year2Path:path,trips:path==='xgen'?(after.organization?.trips||[]).filter(t=>TRAVEL_MONTHS.includes(Number(t.month))):[]}}}return release(after)}
export function serializeState(s){return JSON.stringify({...release(s),updatedAt:Date.now()})}
export function parseSavedState(raw){if(!raw)return null;try{const value=JSON.parse(raw),compat=JSON.stringify({...value,releaseVersion:'1.0',v1SaveVersion:'1.0'}),old=base.parseSavedState(compat);if(!old)return null;let merged={...old,...value,organization:{...(old.organization||{}),...(value.organization||{})},economy:{...(old.economy||{}),...(value.economy||{})},career:{...(old.career||{}),...(value.career||{})},campaignOutcome:value.campaignOutcome||old.campaignOutcome||{xgenByMonth12:false}};const rows=Object.values(merged.settlements||{}).filter(r=>Number(r.month)<=12),hit=rows.some(r=>n(r.currentTGV||r.tgv)>=XGEN_SINGLE_MONTH_TARGET);merged={...merged,career:{...(merged.career||{}),xgenQualified:hit,xgenQualifiedSingleMonth:hit,xgenCertified:hit,xgenCertified1b:hit,xgenQualificationRule:hit?'single-month':null},campaignOutcome:{...(merged.campaignOutcome||{}),xgenByMonth12:!!(merged.campaignComplete?hit:merged.campaignOutcome?.xgenByMonth12)}};if(merged.organizationMode)merged=year2(merged);return release(merged)}catch{return null}}
