# Route Guard — /xircle/start/

Job: เปลี่ยน insight เป็น action เล็ก ๆ วันนี้ โดยให้เลือก 1 อย่าง.

Entry: จาก Xircle/support flow
Exit: สมุดแมวขาว / White Cat Care หรือกลับเส้นหลักตาม context

Must preserve: ไม่แก้ทุกอย่างพร้อมกัน; ไม่ hard sell. User-facing ใช้ `สมุดแมวขาว` / `White Cat Care`. เมื่อ flow พาออกจาก myClover ไปสมุดจริง ต้องไป `https://teambook.me/*` โดยตรง; `/xty/` ใช้ได้เฉพาะ internal repo/compatibility implementation.

Known drift: ค้น `XTY` / `ตี้` และ public `/xty/` handoff ก่อน release.

QA: ทุก choice ใช้ได้, CTA ไม่กระโดด, mobile อ่านง่าย. อ่าน global source/index ก่อนแก้.
