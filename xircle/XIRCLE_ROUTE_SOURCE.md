# Xircle Route Source — one current experience

Updated: **2026-09-10**. The owner requires **V3 only**: cinematic woman/white-cat world, physical meal capture, assembled day, seven-night pattern, contextual Teem + Ako interpretation, then a real appointment invitation. Canonical source: `6fab7897` (`feat/xircle-experience-v3`), merged as `e7b533a7` into remote main (`1984724c`) with identical V3 runtime files.

The prior `85f5b2ac` selection was V2, incorrectly treated as current because local remote refs were stale. That selection and its neutral `experience.js` / `experience.css` files are superseded. Keep the explicit `experience-v3.js` / `experience-v3.css` provenance; do not restore V1/V2 engines.

## Current visitor journey

**เลือกคืนตัวอย่าง → ถ่ายมื้อตัวอย่าง → เลือกการขยับ → ดูหลายวัน → เติมบริบท → นัดดูเรื่องจริงด้วยกัน**

The persistent day record visibly accumulates the visitor's example choices. The same sample data produces a different prepared next step when its context changes. Teem + Ako's role is demonstrated before the invitation to meet.

- Main entry: `/xircle/`; case variants such as `/Xircle/` are aliases of this same route.
- Main next step: `/meet/?intent=health&from=xircle&open=booking`.
- The experience has no registration/app-download CTA. Its quiet optional continuation is `/xircle/learn/` after the payoff.
- Compass may carry an explicitly selected bounded subject such as sleep, move or food. Fictional demo values never become personal measurements in a booking.
- `#appointment` and compatibility `#start` open the appointment invitation. Neither proves that a sample or external action was completed.
- Useful articles and source documents remain independently reachable.

The former mandatory sequence through Human Care, X-VISOR, RoutineX and White Cat is retired. Do not restore it from an August blueprint or old route guard.

## Canonical implementation

| Responsibility | Current files |
|---|---|
| One sample-day experience | `index.html`, `experience-v3.js`, `experience-v3.css`, `assets/v3/`, selected existing V5 and Meet imagery |
| Bounded routing and read-only invitation compatibility | `route-contract.js` |
| Old URL compatibility shells | `legacy-entry.js` plus the eight entries in `ROUTE_INDEX.md` |
| Reference navigation, artwork loading and responsive links | `reference.js`, `reference.css` |
| Existing article data and rendering | `data/library-founder-v1.js`, `data/library-simple-v1.js`, inline article/search code |
| Typeface and retained article styling | `_shared/typography.css`, existing reference CSS |

No separate worker, dev-server implementation, old state gate, scene engine or Compass overlay selects another version. Selected `assets/v5/*` images remain dependencies of V3 and reference material; `_shared/v5.css` remains reference styling. These asset names do not indicate another active experience.

## Old routes and invitations

`start`, `ghost`, `care`, `opportunity` and `routinex` resolve to the latest root. `explore`, `circle` and `care/party` resolve to its `#start` section. The complete map is [ROUTE_INDEX.md](ROUTE_INDEX.md).

The compatibility helper accepts only known bounded parameters. It can read a valid saved invitation without changing the stored record. The latest page presents explicit join/create actions. It does not open an external notebook automatically.

Existing keys remain untouched by consolidation: `xircle.local.v1`, `xircle.session.v1`, `mc:xircle:compass-action:v1`, the Front Door keys and all other historical state. Preserve old achievements and invitations. Never set fake completion flags to unlock a link, clear history, or send raw localStorage.

V3 choices stay in memory; returning from Meet uses `#appointment`, with no new storage key. Retired V1/V2 progress/session keys, including `xircle.demo.v2.resume`, remain intact and are not used to restore V3. The bounded invitation adapter may read an existing saved invitation without changing legacy progress. A link click never proves a confirmed appointment or a health result.

## Reference content and product boundaries

`learn`, `learn/topic`, `hardware`, `products` and the `doc` index keep their content. Their current shared helper has no progress gate and does not write storage. Deeper documents retain their independent rendering.

- The website demonstrates use and interpretation; actual tracking and device functions belong to Xircle App.
- Sample sleep, food, movement and contextual choices remain labeled examples. The assembled day and seven-night pattern show fictional records, not a calculated medical or health score.
- Product, medical, income and formula claims were not revised by this consolidation. Verify current sources before making a new claim.
- No diagnosis, automatic health sharing or guaranteed outcome is introduced.
- Original myClover logo and favicon remain exact assets.
- User-facing Thai naming remains `สมุดแมวขาว` / `White Cat Care`; existing technical route or key names are preserved.

## Required verification

Check mobile and desktop: direct/case/old-link entry; all six actions and their visible payoffs; editing and restart; prepared context variants; real Meet handoff; back/refresh continuation; invalid and saved invitation inputs; storage failure; direct articles, search, media and source links. The main path must not replay the retired journey or require registration/purchase.

## Historical reference archive

Exact pre-consolidation HTML and Markdown were archived before changes, including uncommitted content:

`/Users/Teem/.codex/visualizations/2026/09/07/01a07b30-0992-7eb3-97dc-9d541a4e7820/archive/xircle-retired-20260910/xircle/`

The archived `XIRCLE_ROUTE_SOURCE.md` preserves the full August route, product mappings, naming and White Cat history. Those historical route instructions are not the current navigation specification. Git history preserves the committed source as well.

The following product/naming excerpts are retained verbatim from that historical source for continuity. They are not a new factual review or instructions to rebuild the retired pages.

### Historical RoutineX reference

## 6.1 A · ABSORB

หน้าที่: วางเรื่องการกิน/การย่อยให้อยู่ใน Routine

ตัวช่วยในส่วนนี้:

- G.U.S.+

---

## 6.2 B · BUILD

หน้าที่: ให้สิ่งที่ทำซ้ำมีโภชนาการรองรับ

ตัวช่วยในส่วนนี้:

- Protein HMB+

ไม่ใช้ผลิตภัณฑ์แทนอาหารหรือพฤติกรรม

---

## 6.3 C · CONTROL

**ส่วนนี้ไม่มีขาย**

C คือหัวใจของ RoutineX:

- การเลือก
- การลงมือ
- ความรู้
- พฤติกรรม

ระบบช่วยเตือน, ช่วยวาง, ช่วยติดตามได้ แต่ไม่มีสินค้าไหนลงมือแทนคนได้

นี่คือแกนสำคัญที่สุดในการทำให้ RoutineX ไม่กลายเป็นหน้าขายสินค้าอย่างเดียว

---

## 6.4 D · DAILY BALANCE

หน้าที่: ชั้นตัวช่วยสำหรับวันจริงที่ไม่ได้สมบูรณ์ทุกวัน

ตัวช่วย:

- Vita Matrix
- AstaMega+

ห้ามสื่อว่า “ทุกคนขาด” หรือ “ทุกคนต้องใช้”

---

## 6.5 Flavor+

หน้าที่: ลดความจำเจเพื่อช่วยให้ Routine ทำต่อได้ง่ายขึ้น

ไม่ใช่การรับประกันผลลัพธ์

---

### Historical product mapping

### Structure

- A → G.U.S.+
- B → Protein HMB+
- C → พฤติกรรม ไม่มีขาย
- D → Vita Matrix / AstaMega+
- Flavor+ → adherence / ลดความจำเจ

แต่ละผลิตภัณฑ์สามารถพาไปข้อมูลเชิงลึกต่อได้

### Historical White Cat naming and cadence

## Naming Canon

คำหลักที่ผู้ใช้เห็น:

**สมุดแมวขาว**

ถ้าจำเป็นต้องเรียกชื่อระบบ:

**White Cat Care**

เลิกใช้ XTY เป็นชื่อ user-facing ในเส้นนี้

`/xty/` อาจยังอยู่ใน technical route / implementation เพื่อรักษาระบบเดิม แต่ไม่ใช่ภาษาหลักที่เล่าให้คนใช้

---

### Checkpoints

- Day 0 — เลือก 1 อย่าง
- Day 3 — เช็กว่าไหวไหม
- Day 7 — ดูด้วยกัน
- Day 14 — ปรับ 1 อย่าง
- Day 21 — รักษาจังหวะ
- Day 28 — ทบทวนแล้วเลือกต่อ

28 วันคือ **care / coaching cadence** ไม่ใช่ deadline ทางสรีรวิทยา
