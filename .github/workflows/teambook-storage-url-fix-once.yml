name: TeamBook storage URL isolation fix once

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/teambook-storage-url-fix-once.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Patch TeamBook database env fallback
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          core = Path('teambook/api/_lib/core.js')
          text = core.read_text()
          old = "const url = process.env.TEAMBOOK_DATABASE_URL || process.env.DATABASE_URL;"
          new = "const url = process.env.TEAMBOOK_DATABASE_URL || process.env.STORAGE_URL;"
          if old not in text:
              raise SystemExit('expected core database fallback not found')
          core.write_text(text.replace(old, new, 1))

          health = Path('teambook/api/health.js')
          text = health.read_text()
          old = "databaseConfigured: !!(process.env.TEAMBOOK_DATABASE_URL || process.env.DATABASE_URL),"
          new = "databaseConfigured: !!(process.env.TEAMBOOK_DATABASE_URL || process.env.STORAGE_URL),"
          if old not in text:
              raise SystemExit('expected health database fallback not found')
          health.write_text(text.replace(old, new, 1))
          PY

          grep -F "TEAMBOOK_DATABASE_URL || process.env.STORAGE_URL" teambook/api/_lib/core.js
          grep -F "TEAMBOOK_DATABASE_URL || process.env.STORAGE_URL" teambook/api/health.js

      - name: Commit fix and remove one-time workflow
        shell: bash
        run: |
          rm -f .github/workflows/teambook-storage-url-fix-once.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add teambook/api/_lib/core.js teambook/api/health.js .github/workflows/teambook-storage-url-fix-once.yml
          git commit -m "fix(teambook): bind database fallback to connected storage"
          git push origin HEAD:main
