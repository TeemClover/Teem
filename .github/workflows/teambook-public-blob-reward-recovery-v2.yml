name: TeamBook Public Blob + Reward Recovery v2

on:
  push:
    paths:
      - '.github/workflows/teambook-public-blob-reward-recovery-v2.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply TeamBook public Blob and reward recovery patch
        run: python teambook/scripts/apply-public-blob-reward-recovery-v2.py

      - name: Verify TeamBook build
        working-directory: teambook
        run: |
          npm ci
          npm run build

      - name: Verify Blob architecture test
        working-directory: teambook
        run: node --test tests/private-image.test.mjs

      - name: Commit clean runtime
        shell: bash
        run: |
          rm -f .github/workflows/teambook-public-blob-reward-recovery.yml
          rm -f .github/workflows/teambook-public-blob-reward-recovery-v2.yml
          rm -f teambook/scripts/apply-public-blob-reward-recovery.py
          rm -f teambook/scripts/apply-public-blob-reward-recovery-v2.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(teambook): recover pending cards and use public Blob"
          git push origin HEAD:main
